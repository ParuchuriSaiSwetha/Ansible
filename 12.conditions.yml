- name: Conditions
  hosts: local
  connection : local
  vars:
    is_live: true
  tasks:
  - name: "trying to print Conditions"
    ansible.builtin.debug:
      msg : "Since is_live is true, i am printing it"