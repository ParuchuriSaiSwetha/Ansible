- name: Installing Packages
  hosts: local
  connection : local
  become : yes
  tasks:
  - name: "Installing Packages"
    ansible.builtin.package:
      name: "{{item}}"
      state: present
    loop:
    - nginx
    - mysql
#ansible-playbook -i inventory.ini 17.loops_install_packages.yaml